<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_height="match_parent"
        android:layout_width="match_parent"
        android:background="@drawable/background"
        android:padding="20dp"
        tools:context=".ui.Settings.SettingsFragment">

    <Switch
            android:id="@+id/setToast"
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:text="Показывать Toast's ?"
            android:padding="10dp"
            tools:ignore="UseSwitchCompatOrMaterialXml"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintVertical_bias="0.1"
            android:checked="true"/>

    <Switch
            android:id="@+id/setExit"
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:text="Выходить из приложения?"
            android:padding="10dp"
            tools:ignore="UseSwitchCompatOrMaterialXml"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:checked="false"
            app:layout_constraintTop_toBottomOf="@+id/setToast"
            app:layout_constraintVertical_bias="0"/>
    <Button
            android:id="@+id/setTheme"
            android:onClick="changeTheme"
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:text="Сменить цвет"
            android:textAllCaps="false"
            android:scrollbarStyle="insideOverlay"
            style="@style/Widget.AppCompat.Button.Colored"
            tools:ignore="OnClick"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/setExit"
            app:layout_constraintVertical_bias="0"/>

</androidx.constraintlayout.widget.ConstraintLayout>